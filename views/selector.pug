extends layout

block content
  h1= title
  - var image_names = ["vanilla", "chocolate", "oreo", "belgian", "walnut"]
  - for(let i = 0; i < 3; i++) {
    - let randIndex = Math.floor(Math.random() * 5)
    console.log(ranIndex)
    img(src=`/images/${image_names[randIndex]}.jpg`, height="300px", width="300px")
  - } 